doctype html
html
  body
    ul
      each post in posts
        li: a(href=post.url)
          h1= post.title
          h2= moment(post.date).fromNow()
          em= post.path
          != post.content
